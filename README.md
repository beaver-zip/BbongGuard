# 🛡️ 뽕가드 (BbongGuard)

**YouTube 가짜뉴스 탐지 AI 확장프로그램**

유튜브 영상의 텍스트, 이미지, 오디오를 종합 분석하여 가짜뉴스와 낚시성 콘텐츠를 탐지합니다.

---

## ✨ 주요 기능

| 모듈 | 분석 내용 | 역할 |
|------|----------|------|
| 📝 **텍스트** | 제목 + 설명 + 자막 → 주장 추출 → RAG 팩트체크 | **핵심 판정** |
| 🖼️ **이미지** | 썸네일 OCR → 낚시성 키워드 탐지 → GPT Vision 심층 분석 | 보조 판정 |
| 🎙️ **오디오** | STT 변환 → 선동성/과장 표현 탐지 | 보조 판정 |

---

## 🔄 분석 파이프라인

```
┌─────────────────────────────────────────────────────────────┐
│  1️⃣ 오디오 분석 (STT)                                       │
│     └─ Naver Clova Speech로 자막 생성                        │
└─────────────────────────────────────────────────────────────┘
                              ↓ transcript 공유
┌─────────────────────────────────────────────────────────────┐
│  2️⃣ 텍스트/이미지 병렬 분석                                  │
│     ├─ 텍스트: 주장 추출 → Tavily 검색 → 임베딩 Re-rank → LLM 판정 │
│     └─ 이미지: 썸네일 OCR → 키워드 필터 → GPT Vision 분석      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  3️⃣ 최종 종합 판정 (Verdict Agent)                          │
│     └─ 텍스트(핵심) + 이미지/오디오(보조) → 가짜뉴스 여부 결정   │
└─────────────────────────────────────────────────────────────┘
```

---

## 🏗️ 프로젝트 구조

```
BbongGuard/
├── server/                     # FastAPI 백엔드
│   ├── main.py                # API 엔트리포인트 (스트리밍)
│   ├── text_module/           # RAG 기반 팩트체크
│   │   ├── claim_extractor.py # 주장 추출 (LLM)
│   │   ├── web_searcher.py    # Tavily 웹 검색
│   │   ├── evidence_ranker.py # 임베딩 기반 Re-ranking
│   │   └── verdict_agent.py   # 판정 및 통합
│   ├── image_module/          # 썸네일 자극성 분석
│   ├── audio_module/          # 선동성 분석
│   └── shared/                # 공통 모듈
│       ├── llm_client.py      # OpenAI 클라이언트
│       ├── source_manager.py  # 화이트/블랙리스트 관리
│       └── schemas.py         # Pydantic 스키마
│
├── extension/                  # Chrome 확장프로그램
└── requirements.txt
```

---

## 🚀 설치 및 실행

### 1. 환경 설정

```bash
# 클론
git clone https://github.com/beaver-zip/BbongGuard.git
cd BbongGuard

# 가상환경
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt
```

### 2. API 키 설정

`.env` 파일 생성:

```ini
# 필수
OPENAI_API_KEY=sk-...
TAVILY_API_KEY=tvly-...

# 선택 (이미지/오디오 분석용)
GOOGLE_APPLICATION_CREDENTIALS_PATH=path/to/credentials.json
NAVER_CLOVA_SPEECH_INVOKE_URL=https://...
NAVER_CLOVA_SPEECH_SECRET_KEY=...
YOUTUBE_API_KEY=...
```

### 3. 서버 실행

```bash
uvicorn server.main:app --host 0.0.0.0 --port 8000
```

---

## 📡 API

### POST `/api/analyze-multimodal`

**Request:**
```json
{
  "video_id": "GrlANJfluvM",
  "title": "영상 제목",
  "description": "영상 설명",
  "published_at": "2024-12-01T00:00:00Z"
}
```

**Response (NDJSON Streaming):**
```json
{"type": "progress", "message": "오디오 분석 중..."}
{"type": "progress", "message": "텍스트/이미지 분석 중..."}
{"type": "result", "data": {
  "final_verdict": {
    "is_fake_news": false,
    "confidence_level": "high",
    "overall_reasoning": "...",
    "text_sources": [{"title": "연합뉴스", "url": "..."}]
  }
}}
```

---

## 🔧 기술 스택

| 분류 | 기술 |
|------|------|
| 백엔드 | FastAPI, Uvicorn |
| LLM | OpenAI GPT-4o / GPT-4o-mini |
| 임베딩 | SentenceTransformer (paraphrase-multilingual-mpnet-base-v2) |
| 웹 검색 | Tavily API |
| 이미지 분석 | Google Cloud Vision API |
| 오디오 분석 | Naver Clova Speech |
| 확장프로그램 | Chrome Extension (Manifest V3) |

---

## ⚠️ 주의사항

- **비용**: OpenAI, Tavily, Clova 등 유료 API 사용에 따른 비용 발생
- **정확도**: AI 분석은 보조 수단이며, 100% 정확성을 보장하지 않음
- **속도**: 영상 길이에 따라 분석에 수 초 ~ 수 분 소요

---

## 📄 라이선스

MIT License